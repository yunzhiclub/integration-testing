server:
  port: 8080

spring:
  datasource:
    url: "jdbc:mysql://localhost:3307/integrationTesting"
    username: "root"
    password: "yunzhi"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    generate-ddl: true
    show-sql: false
    properties:
      hibernate:
        dialect: "org.hibernate.dialect.MySQL8Dialect"
    hibernate:
      ddl-auto: create-drop

logging:
  level:
    club:
      yunzhi: info
    root: info
